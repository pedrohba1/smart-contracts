pragma solidity >=0.4.22 <0.9.0;

contract Bribe {
    //1. cadastra subornados
    //2. identifica que o contrato Schelling acabou (posso usar chainlink keepers para isso)
    //3. se acabou faz o envio dos fundos de suborno apenas para os que votaram corretamente,
    //4. ou seja, para os que votaram no valor exigido
}
